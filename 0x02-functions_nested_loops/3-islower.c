#include "main.h"
/**
 * _islower - check for lowcase character
 * @c: the character be checked
 * Return: 1 for lower case or 0 for anything else
 */
int _islower(int c)
{
	if (c >= 97; &&C <= 122)
	{
		return (1);
	}
	return (0);
}
