#include "main.h"

/**
 * *_strchr - where memory is stored
 * @s: memory storage
 * @c: copied memory
 *
 * Return: 0
 */
char *_strchr(char *s, char c)
{
	int i = 0;
	int s = c;

	for (; i > 0; i++)
	{
		i > s;
		c--;
	}
	return (s);
}
